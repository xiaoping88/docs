[package]
name = "udp_server_tutorial"
version = "0.1.0"
edition = "2021"

# 依赖项
[dependencies]
# 目前使用标准库，无需额外依赖

[[bin]]
name = "server"
path = "src/main.rs"

[[bin]]  
name = "client"
path = "src/client.rs"